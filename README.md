# Hiree-talent-services
Hiree is a website like fiverr.Any household services can be offered

Steps to run

=>Frontend:-

cd frontend && npm install && npm run start

=>Backend:-

cd backend && npm install && node server.js
